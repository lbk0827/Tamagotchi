# v0.1 QA 결과 리포트 (1차)

작성일: 2026-04-05
환경: Codex 실행 환경(현재 GUI 제약 존재)

## 1) 자동 검증 결과

### PASS
- [x] TypeScript 빌드 성공
  - 실행 명령: `npm run build`
  - 결과: 컴파일/자산 복사 정상 완료

### BLOCKED (환경 제약)
- [ ] `npm run dev`로 Electron 창 실행
  - 실행 명령: `npm run dev`
  - 결과: Electron 프로세스가 `SIGABRT`로 종료
  - 참고: 현재 실행 환경의 GUI 제약 가능성이 높음 (코드 컴파일 자체는 성공)

## 2) 코드 기반 검토 결과
- [x] OpenAI 호출 실패 시 폴백 응답 처리 존재
- [x] 상태 저장 실패 시 `false` 반환 및 렌더러 안내 문구 처리 존재
- [x] 투명 영역 클릭 통과 로직(`setIgnoreMouseEvents`) 존재
- [x] 상태 시스템 감쇠/액션 반영 로직 존재
- [x] 오프라인 시간 반영 로직 존재

## 3) 수동 검증 필요 항목 (로컬 PC에서 확인)
아래 항목은 실제 GUI 상호작용이 필요합니다.

- [ ] 알 모양 창 표시/항상 위 유지
- [ ] 알 내부 클릭 가능 + 외부 투명 영역 클릭 통과
- [ ] 드래그 이동, 숨기기, Tray 재표시
- [ ] Feed/Play/Clean/Sleep 수치 반영
- [ ] 1분 경과 감쇠 확인
- [ ] 앱 재시작 후 상태 복구
- [ ] OpenAI 키 설정 시 실제 AI 응답
- [ ] 키 미설정 또는 네트워크 실패 시 폴백 응답

## 4) 다음 권장 순서
1. 로컬 환경에서 `npm run dev` 실행 후 GUI 수동 체크 진행
2. `Docs/v0.1_QA_Test_Plan.md` 체크박스 업데이트
3. 남은 이슈 반영 후 `v0.1 RC` 태그 준비
